server:
  port: 8081
  error:
    include-message: always
    include-binding-errors: never
    include-stacktrace: never
    include-exception: false

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/plazoleta
    username: root
    password: root

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

appDescription: "Microservicio de restaurantes para Plaza de Comidas"
appVersion: "1.0.0"

microservices:
  usuarios:
    url: http://localhost:8080